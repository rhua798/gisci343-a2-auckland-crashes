[project]
name = "gisci343-a2-auckland-crashes"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12" 
dependencies = [
    "shiny", "shinywidgets", "ipyleaflet",
    "pandas", "matplotlib", "pyproj", "shinyswatch",
]
